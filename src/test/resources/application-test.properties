spring.application.name=openmrs-contrib-flink

# Use H2 in-memory database for tests
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MYSQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA/Hibernate Configuration for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Secrets encryption key (test-only)
secrets.encryption-key=HdF3lssbjZz2Is7KXLs9mx9O6l+iW/tOFEaPF/4AGBI=

# JWT Configuration (test-only)
jwt.secret=dGhpcyBpcyBhIHNhbXBsZSBzZWNyZXQga2V5IGZvciBqd3Q=
jwt.expiration-ms=86400000

# Default admin credentials
admin.default-username=admin
admin.default-password=admin123